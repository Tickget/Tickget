name: Ticketing Server CI/CD

on:
  push:
    branches:
      - dev
    paths:
      - 'Backend/ticketing-server/**'
      - '.github/workflows/ticketing-server.yml'

jobs:
  build-dev:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      service_name: ticketing-server
      service_path: Backend/ticketing-server
      docker_image: kkaebu/ticketing-server
      environment: dev
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      GH_PAT: ${{ secrets.GH_PAT }}
